const allowedCors = [
  'https://azannik.nomoredomains.rocks',
  'http://azannik.nomoredomains.rocks',
  'localhost:3000',
];

const DEFAULT_ALLOWED_METHODS = 'GET,HEAD,PUT,PATCH,POST,DELETE';

export { allowedCors, DEFAULT_ALLOWED_METHODS };
